info:
  description: Spring Cloud Config Server

spring:
  application:
    name: config-server

encrypt:
  keyStore:
    location: classpath:keystore.jks
    password: ${KEYSTORE_PASSWORD:foobar} # foobar
    alias: test