server:
  port: 8080

spring:
  application:
    name: minion-front
  cloud:
    config:
      discovery:
        enabled: true
   
eureka:
  client:
    serviceUrl:
      defaultZone: http://Sirius:8761/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 10       

