server:
  port: 8881

spring:
  application:
    name: minion-characters
  cloud:
    config:
      discovery:
        enabled: true
   
   
eureka:
  client:
    serviceUrl:
      defaultZone: http://minion-eureka-server:8761/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 10       
